export class Car {
    public mark: string;
    public model: string;
    public colors: string[];

    constructor(mark: string, model: string, colors: string[]){
        this.mark = mark;
        this.model = model;
        this.colors = colors;
    }
}